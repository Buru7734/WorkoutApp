const hashedPassword = bcrypt.hashSync(password, 10);
try {
  const newUser = User({ username, password: hashedPassword });
  await newUser.save();
  return res.status.send("Sign up successful!");
} catch (error) {
  console.log(error);
  return res.status(500).send("Error signing up user.");
}
